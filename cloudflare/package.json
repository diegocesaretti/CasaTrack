{
  "name": "casatrack-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply casatrack --local",
    "migrate:remote": "wrangler d1 migrations apply casatrack --remote"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^4.28.1",
    "@cloudflare/workers-types": "^5.20260820.1"
  }
}
